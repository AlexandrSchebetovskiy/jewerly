import {PageComponent} from '@core/PageComponent'
export class StorePageComponent extends PageComponent {
  constructor(options) {
    super(options.components, 'StorePage', options.data, options.param)
  }
}
